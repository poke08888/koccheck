{
  "name": "livescope-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LiveScope — KOC livestream analytics backend (Express + node:sqlite)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "seed": "node --no-warnings seed.js",
    "start": "node --no-warnings src/server.js",
    "dev": "node --no-warnings --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "pg": "^8.22.0"
  }
}
